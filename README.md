## Setup
1. Clone the repo
2. Run `npm install`
3. Create `.env` from `.env.example` and fill in your Supabase keys
4. Run `npm run dev`

## Environment Variables
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key